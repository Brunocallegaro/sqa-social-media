{
  "name": "sqa-social-media-tests",
  "version": "1.0.0",
  "description": "Testes E2E e de API para o projeto SQA Social Media",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test e2e/",
    "test:api": "newman run api/collection.json --env-var baseUrl=http://localhost:8080",
    "test": "playwright test e2e/"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "newman": "^6.1.3"
  }
}
